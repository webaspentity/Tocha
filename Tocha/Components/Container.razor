<div class=@((Class is null) ? "container" : $"container {Class}")>
    @ChildContent
</div>

@code{
    [Parameter]
    public string? Class { get; set; }
    [Parameter]
    public RenderFragment? ChildContent { get; set; }
}